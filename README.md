CarteBlanche bundle - AutoObject
===============================

The bundle to build the automatic objects from an object map.

This package is a bundle for the [CarteBlanche framework](https://github.com/php-carteblanche/carteblanche).
